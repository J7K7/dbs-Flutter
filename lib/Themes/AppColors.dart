import 'package:flutter/material.dart';

const primary100 = Color(0xFF1e2022); // main button color
const primary200 = Color(0xFF343a40); // main patti (dark one step down)
const primary300 = Color(0xFF495057); // small button color

const accent100 = Color(0xFFCED4DA); // middle - 1 dark side
const accent200 = Color(0xFFADB5BD); // middle
const accent300 = Color(0xFF6C757D); // middle + 1 dark side
const text100 = Color(0xFF1e2022); // Heading text (dark)
const text200 = Color(0xFF52616b); // small text (dark)
const text300 = Color(0xFFF8F9FA); // text inside button (light)
const bg100 = Color(0xFFE9ECEF); // main bg (light)
const bg200 = Color(0xFFDEE2E6); // card color
// const bg300 = Color(0xFF343a40); // main patti (dark one step down)
